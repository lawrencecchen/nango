/// <reference types="react" />
import type { Flow } from '../../../types';
export interface FlowProps {
    flow: Flow;
}
export default function FlowCard({ flow }: FlowProps): import('react').JSX.Element;
