/// <reference types="react" />
import type { FlowEndpoint } from '../../../types';
export default function EndpointLabel({ type, endpoint }: { type: string; endpoint: string | FlowEndpoint }): import('react').JSX.Element;
